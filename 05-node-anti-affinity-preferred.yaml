apiVersion: v1
kind: Pod
metadata:
  name: with-node-anti-affinity-preferred
spec:
  affinity:
    nodeAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 1
        preference:
          matchExpressions:
          - key: project
            operator: NotIn
            values:
            - expense
  containers:
  - name: nginx-with-node-affinity
    image: nginx

#Node Anti affinity also created using nodeAffinity only.

# Label the node:
# Quick checklist for this to work:
# Label your node:
# kubectl label node ip-192-168-14-127.ec2.internal project=expense

# Create the pod with your YAML file:
# kubectl apply -f 05-node-anti-affinity-preferred.yaml

# Check pod placement and node labels:
# kubectl get pods -o wide
# kubectl get nodes --show-labels
